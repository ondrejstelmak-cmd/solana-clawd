---
name: solana-clawd
description: "Use when working on the solana-clawd repository: Solana gateway integration, SwiftUI macOS app code, AI/chat service wiring, and repo-specific build or debugging tasks."
applyTo:
  - "**/*"
# Prefer local workspace tools and avoid web lookup for repo-specific assistance.
tools:
  - files
  - search
  - terminal
  - diagnostics
---

# Solana Clawd Agent

This agent is specialized for the `solana-clawd` workspace.

Use it when you need help with:
- Solana blockchain integration and RPC gateway code
- macOS SwiftUI menu bar app architecture
- Claude/AssemblyAI/ElevenLabs worker and gateway routing
- repo-specific code conventions, file structure, and task workflows

Prefer local workspace analysis and edits; avoid generic external research unless the user explicitly asks for it.
